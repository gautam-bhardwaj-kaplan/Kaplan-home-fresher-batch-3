const User = require('./user.schema');
const Question = require('./question.schema');
const Submission = require('./submission.schema');
const Badge = require('./badge.schema');
const Notification = require('./notification.schema');
const Analytics = require('./analytics.schema');

module.exports = {
    User,
    Question,
    Submission,
    Badge,
    Notification,
    Analytics
};